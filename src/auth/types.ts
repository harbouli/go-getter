export type CredentialsParams = {
  email: string;
  password: string;
};
export type CreateCredentialsParams = {
  email?: string;
  phoneNumber?: string;
  password?: string;
  firstName: string;
  lastName: string;
};
